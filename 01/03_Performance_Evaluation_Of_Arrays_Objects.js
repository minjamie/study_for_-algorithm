let instructor = {
    firstName: 'Minjae',
    isInstructor: true,
    favoriteNums: [1, 2, 3, 4]
}

Object.keys(instructor);
Object.values(instructor);

let names = ['a', 'b', 'c ']

names.push('d')